{% extends "base.html.twig" %}


{% block body %}



<h3>Ajouter une tache a la liste {{list.name}}</h3>






<div class="container">
  {{form_start(formulaire)}}
  {{form_row(formulaire.title)}}
  <input type="submit" value="Créer" class="btn btn-success mt-2">
  {{form_end(formulaire)}}
</div>



{% endblock body %}